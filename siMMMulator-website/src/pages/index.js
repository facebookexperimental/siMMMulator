/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @format
 */

import React from 'react';
import clsx from 'clsx';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './styles.module.css';

const features = [
  {
    title: 'Simulate and Test Various Scenarios',
    imageUrl: '',
    description: (
      <>
        siMMMulator can allow you to test out how your MMMs respond to various scenarios.
        For example, how would your MMM handle volatile changes in Cost per Impressions?
        How would MMMs handle changes in ad spend?
      </>
    ),
  },
  {
    title: 'Quantify the Value of Innovations in your MMM',
    imageUrl: '',
    description: (
      <>
        Test your current MMM with data you created from siMMMulator to help you understand how well your MMM can recover those ground truth ROIs we simulated the data on.
        Then, run your MMM with an improvement and see how much closer the improved model got to those ground truth ROIs.
      </>
    ),
  },
  {
    title: 'Private and Kept on your Local Computer',
    imageUrl: '',
    description: (
      <>
        All data simulated can be kept on your local computer and not sent anywhere. In fact, siMMMulator can be run without the internet (after it is installed).
      </>
    ),
  },
];

function Feature({imageUrl, title, description}) {
  const imgUrl = useBaseUrl(imageUrl);
  return (
    <div className={clsx('col col--4', styles.feature)}>
      {imgUrl && (
        <div className="text--center">
          <img className={styles.featureImage} src={imgUrl} alt={title} />
        </div>
      )}
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}

export default function Home() {
  const context = useDocusaurusContext();
  const {siteConfig = {}} = context;
  return (
    <Layout
      title={`${siteConfig.title}`}
      description="Description will go into a meta tag in <head />">
      <header className={clsx('hero hero--primary', styles.heroBanner)}>
        <div className="container">
          <h1 className="hero__title">{siteConfig.title}</h1>
          <p className="hero__subtitle">{siteConfig.tagline}</p>
          <div className={styles.buttons}>
            <Link
              className={clsx(
                'button button--outline button--secondary button--lg',
                styles.getStarted,
              )}
              to={useBaseUrl('docs/set_up')}>
              Get Started
            </Link>
          </div>
        </div>
      </header>
      <main>
        {features && features.length > 0 && (
          <section className={styles.features}>
            <div className="container">
              <div className="row">
                {features.map(({title, imageUrl, description}) => (
                  <Feature
                    key={title}
                    title={title}
                    imageUrl={imageUrl}
                    description={description}
                  />
                ))}
              </div>
            </div>
          </section>
        )}
      </main>
    </Layout>
  );
}
